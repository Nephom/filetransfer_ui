---
id: TASK_0001
state: completed
phase: done
created_at: 2026-08-16T11:08:58.961Z
updated_at: 2026-08-16T11:41:46.730Z
title: Rewrite `issue-194-mobile-ui-task-list.csv` (same CSV schema: `task_id,test_case_id,status,priority,mode,implementation_task,existing_surface,expected_behavior,files,verification,blocked_by`) so that, given the current state of the code (statuses already marked DONE/WONT_DO/PENDING as recorded in the existing file), the remaining/rewritten task set converges the desktop app (`fileapi_ui/`) on one unified, consistent visual/interaction style, following the design rules already established under `.planning/` (the design-system research and constraints referenced by earlier tasks such as T-001's `ui-ux-pro-max` research and the token/breakpoint/profile work in T-008–T-150). The rewritten CSV must ensure: (1) no UI element uses fixed/hardcoded absolute pixel positions or sizes — all sizing must flow from the shared token system (`fileapi_ui/src/styles/tokens.css`) and the shared mobile/viewport resolver (`fileapi_ui/src/styles/breakpoints.ts`); (2) the "Auto" profile uses fluid typography/control scaling (per T-028/T-029's clamp-based approach) so all font sizes and control dimensions scale continuously as the window is resized, with no discrete breakpoint jumps; (3) the "Large" profile (previously called "Mobile", per T-058's "Mobile (Touch Friendly)" label and the `uiProfile`/`uiDensity` settings in `fileapi_ui/src/main.tsx`) scales every text, control, icon, and spacing value up uniformly, not just body text, so the whole UI enlarges together; (4) the entire app remains usable and readable at a minimum viewport of 800×600 (per the existing 800×600 verification tasks T-092–T-098, T-125–T-126, T-137) for both Auto and Large profiles, with no clipped controls, unreadable ellipsis, or horizontal overflow (per T-134). The rewrite should preserve traceability: every row keeps a unique `task_id`/`test_case_id` pair, correct `blocked_by` dependency chains, and coverage of every changed file, consistent with the audit tasks already in the list (T-145–T-147).
label: Rewrite mobile UI task list CSV — unify desktop app styling (tokens,…
---

## raw prompt

木標 => 請參閱 @issue-194-mobile-ui-task-list.csv , 我要以現在的代碼狀態  
，把這個 csv 檔重新撰寫，目標 統一整合的風格檔，盡可能採用 @.planning/ 裡面的設計規範, 讓整個桌面應用的風格不會散落一地，不要固定寫死絕對位置，所有的字體大小都要能隨著視窗放大就放大也就是Auto profile; Large profile(原先的mobile) 要能把所有一切都  
放大處理，且要符合最低 800 * 600 也能觀看的配置

## refined prompt

I can't enumerate `.planning/` contents with the available tool (no directory listing capability), so I'll proceed based on the CSV content itself, which references `.opencode/skills/ui-ux-pro-max` and other concrete artifacts already documented in the task list.

GOAL
Rewrite `issue-194-mobile-ui-task-list.csv` (same CSV schema: `task_id,test_case_id,status,priority,mode,implementation_task,existing_surface,expected_behavior,files,verification,blocked_by`) so that, given the current state of the code (statuses already marked DONE/WONT_DO/PENDING as recorded in the existing file), the remaining/rewritten task set converges the desktop app (`fileapi_ui/`) on one unified, consistent visual/interaction style, following the design rules already established under `.planning/` (the design-system research and constraints referenced by earlier tasks such as T-001's `ui-ux-pro-max` research and the token/breakpoint/profile work in T-008–T-150). The rewritten CSV must ensure: (1) no UI element uses fixed/hardcoded absolute pixel positions or sizes — all sizing must flow from the shared token system (`fileapi_ui/src/styles/tokens.css`) and the shared mobile/viewport resolver (`fileapi_ui/src/styles/breakpoints.ts`); (2) the "Auto" profile uses fluid typography/control scaling (per T-028/T-029's clamp-based approach) so all font sizes and control dimensions scale continuously as the window is resized, with no discrete breakpoint jumps; (3) the "Large" profile (previously called "Mobile", per T-058's "Mobile (Touch Friendly)" label and the `uiProfile`/`uiDensity` settings in `fileapi_ui/src/main.tsx`) scales every text, control, icon, and spacing value up uniformly, not just body text, so the whole UI enlarges together; (4) the entire app remains usable and readable at a minimum viewport of 800×600 (per the existing 800×600 verification tasks T-092–T-098, T-125–T-126, T-137) for both Auto and Large profiles, with no clipped controls, unreadable ellipsis, or horizontal overflow (per T-134). The rewrite should preserve traceability: every row keeps a unique `task_id`/`test_case_id` pair, correct `blocked_by` dependency chains, and coverage of every changed file, consistent with the audit tasks already in the list (T-145–T-147).

CONSTRAINTS
- Do not change the CSV's column schema (`task_id,test_case_id,status,priority,mode,implementation_task,existing_surface,expected_behavior,files,verification,blocked_by`) — preserve it exactly.
- Preserve the status of tasks already marked `DONE` or `WONT_DO` in the current file as the authoritative baseline of "current code state"; do not silently revert or re-open work that is already DONE unless the code inspection shows it regressed.
- Preserve all existing concrete identifiers verbatim where they still apply: file paths (`fileapi_ui/src/main.tsx`, `fileapi_ui/src/styles/tokens.css`, `fileapi_ui/src/styles/breakpoints.ts`, `fileapi_ui/src/desktop-ui.css`, `fileapi_ui/src/login.css`, `fileapi_ui/src/rest-api.tsx`, `fileapi_ui/src/proxmox-vnc.tsx`, `fileapi_ui/src/explorer-parity.css`, `fileapi_ui/src/styles/commandbar.css`, etc.), task IDs (`T-###`), test case IDs (`TC-###`), and threshold values (1024×768 mobile boundary, 800×600 minimum viewport, 40px minimum control height, 16px/14px minimum text sizes) unless the rename to "Large profile" explicitly requires updating profile-name references (e.g., "Mobile" → "Large" label text and related identifiers such as `uiProfile`, `data-ui-profile`, CSS class names) — call out any such rename explicitly rather than silently changing behavior.
- Do not modify `package.json`, `fileapi_ui/package.json`, `fileapi_ui/src-tauri/Cargo.toml`, `fileapi_ui/tsconfig.json`, or `fileapi_ui/vite.config.ts` as part of this CSV rewrite; this task is scoped to the CSV task list (and, when implemented, the referenced source/style files), not to build/dependency configuration.
- Do not introduce new hardcoded absolute positioning or fixed pixel sizes anywhere in the rewritten task descriptions' expected behavior — every sizing-related task must specify token-driven or clamp/fluid-scale behavior instead.
- Maintain one-to-one `task_id`-to-`test_case_id` uniqueness and full changed-file coverage, per the existing audit tasks (T-145, T-146).
- Do not fabricate design rules that are not present in `.planning/` or in the current codebase; where `.planning/` content could not be located/read, flag this as a known unknown rather than inventing design-system specifics.

KNOWN-UNKNOWNS
- The `.planning/` directory's actual file contents could not be located/read with the available tooling (no directory-listing capability, and no filename in the CSV or package files points to a specific `.planning/*.md` file). Which exact file(s) under `.planning/` contain the authoritative design rules to follow, and can the user provide their exact path(s) or contents?
- Is "Large profile" simply a rename of the existing "Mobile (Touch Friendly)" profile (T-058, `uiProfile`), or a new third profile distinct from Auto and Mobile? Should all existing "Mobile" identifiers (class names, setting values, labels like T-058's "Mobile (Touch Friendly)") be renamed to "Large", or only the user-facing label?
- Should the already-DONE/WONT_DO tasks (e.g., T-030–T-032, T-141, T-152 which fixed density tiers as WONT_DO) be reopened now that "Large profile" must scale "everything," given T-152 explicitly marked making density tiers visually distinct as WONT_DO?
- Should the rewritten CSV keep all currently PENDING rows (T-033, T-061–T-080, T-083–T-131, T-153–T-159) as-is, or should they be re-scoped/reworded to explicitly reference token-driven/no-fixed-position/fluid-scale requirements?
- What is the expected output format/filename — should the rewrite overwrite `issue-194-mobile-ui-task-list.csv` in place, or produce a new file, and should row count/task IDs be preserved or may new tasks be added/removed for the "unified style file" goal?
- Does "統一整合的風格檔" (unified integrated style file) mean the CSV itself should be restructured as a style-guide artifact, or does it mean the underlying CSS/token files should converge into one unified stylesheet architecture (with the CSV merely tracking that work)?

EXTERNAL-DEPENDENCIES

## verified tooling

cd fileapi_ui && tsc --noEmit
cd fileapi_ui && tsc --noEmit
cd fileapi_ui && tsc --noEmit && vite build
cd fileapi_ui && vite
node --test --test-concurrency=1

## research

FILES
That's sufficient — I have the full picture now. Producing the FILES section.

issue-194-mobile-ui-task-list.csv  the CSV task list to be rewritten (schema + all existing rows/statuses)
.planning/ui-baseline/SUPERSEDED-DENSITY-TASKS.md  records WONT_DO decision for density-tier tasks (T-030/031/032/141/152) and their blocked_by cleanup
.planning/ui-baseline/BASELINE_COMMIT.txt  commit hash the baseline audit was taken against
.planning/ui-baseline/CSS-IMPORTS.md  authoritative map of every CSS file, load order, and owning task
.planning/ui-baseline/DROPDOWN-INVENTORY.md  inventory of native selects/custom menus and their owning migration tasks (T-050, T-061-076)
.planning/ui-baseline/GLYPH-INVENTORY.md  inventory of Unicode glyphs and their owning icon-migration tasks (T-040-049)
.planning/ui-baseline/MEDIA-QUERIES.md  inventory of every @media rule, grouped by profile-boundary vs. component-intrinsic vs. accessibility rules (T-036-039)
.planning/ui-baseline/IN-PROGRESS-T040-T049.md  session log of completed icon-migration batch, notes on what remains PENDING
.planning/ui-baseline/AUDIT-T042-T074-catchup.md  audit reconciling CSV DONE/PENDING status against actual code state for T-042/T-050-054/T-072-074
.planning/ui-research/design-system/nfterm/MASTER.md  raw ui-ux-pro-max design-system search output (colors/typography largely NOT adopted, see ACCESSIBILITY-RULES.md)
.planning/ui-research/design-system/nfterm/ACCESSIBILITY-RULES.md  authoritative subset of design rules actually adopted for this project (focus, contrast, motion, cursor, keyboard nav rules referenced by T-001, T-078, T-079, T-133)
fileapi_ui/src/main.tsx  DesktopApp/LoginScreen/Settings root component; owns uiProfile/uiDensity settings, mobile layout resolver usage, profile label text ("Mobile (Touch Friendly)"), commandbar, native selects targeted for Dropdown migration, collapse controls
fileapi_ui/src/styles/tokens.css  central design-token file (color/type/spacing/control/focus/motion/overlay/dropdown/icon tokens) — all sizing must flow from here per the task's core constraint
fileapi_ui/src/styles/breakpoints.ts  shared mobile viewport resolver and mobile width/height constants (1024/768) used by both TS and CSS-mirroring logic
fileapi_ui/src/desktop-ui.css  Auto fluid typography/control scale (clamp-based, T-028/T-029), former density-tier rules (now WONT_DO), mobile text/control overrides
fileapi_ui/src/styles/mobile-ui.css  shared mobile text/control-height/focus-ring floors (T-020-023, T-033-034)
fileapi_ui/src/login.css  login screen styles incl. profile breakpoint media query (T-036), profile Dropdown migration target
fileapi_ui/src/tls.css  contains a duplicate login mobile-text media query cross-referenced with login.css (T-150)
fileapi_ui/src/explorer-parity.css  Location/SSH/REST/VNC shared layout styles incl. collapse controls, split panes, breakpoint stacking rules (T-038, T-084-098, T-149)
fileapi_ui/src/location-control.css  Location command bar/collapse control styles
fileapi_ui/src/styles/commandbar.css  commandbar layout, overflow-menu styles (T-092-098, T-125, T-153-160)
fileapi_ui/src/rest-api.tsx  REST workspace component; native selects to migrate (T-064-071), vendor/entry menus, disclosures, collapse controls
fileapi_ui/src/rest-api.css  REST workspace styles incl. collapse-pane CSS (T-151), disclosure sections, narrow-width rules
fileapi_ui/src/proxmox-vnc.tsx  VNC workspace component; entry/node/VM selects (already migrated to Dropdown), collapse controls, toolbar actions
fileapi_ui/src/proxmox-vnc.css  VNC workspace styles incl. collapse/toolbar rules, narrow-width rules
fileapi_ui/src/features/vnc/VncWorkspaceController.tsx  VNC secondary-pane collapse/restore logic (T-119-120)
fileapi_ui/src/context-picker.tsx  ContextPicker custom listbox component (glyphs done, restyle with Dropdown tokens pending, T-075)
fileapi_ui/src/context-picker.css  ContextPicker styles to align with shared Dropdown tokens (T-075, T-077)
fileapi_ui/src/ui/Dropdown.tsx  shared Dropdown component (created; keyboard nav, focus return, outside-click handled)
fileapi_ui/src/ui/dropdown.css  shared Dropdown stylesheet reading token-driven trigger/menu/option/focus styles (T-054, T-078, T-079)
fileapi_ui/src/ui/MobileChoiceMenu.tsx  mobile "current choice" menu component used across REST/VNC/mode-switch (T-076, T-133)
fileapi_ui/src/ui/mobile-choice-menu.css  MobileChoiceMenu styles pending restyle with shared Dropdown tokens (T-076)
fileapi_ui/src/ui/icons.tsx  shared inline SVG icon set with token-driven default sizing (T-040/T-041)
fileapi_ui/src/ui/overlay-registry.ts  overlay z-index layer ordering (T-015)
fileapi_ui/src/styles/overlays.css  overlay z-index/portaled surface styles, modal overflow media rules (T-039, T-077)
fileapi_ui/src/styles/surface-overrides.css  narrow-viewport surface override rules, login short-height rules (T-037, T-039)
fileapi_ui/src/styles/theme-overrides.css  four color-theme definitions (bridge/graphite/emerald/tech-silver) verified against Dropdowns (T-139)
fileapi_ui/src/styles/theme.ts  theme selection logic referenced by Settings color-theme Dropdown (T-061, T-139)
fileapi_ui/src/styles/mode-switcher.css  titlebar mode-switch control styles, dead mobile-trigger CSS removed (T-123, T-148)
fileapi_ui/src/account-menu.css  account menu (titlebar) dropdown styles (T-077, T-124)
fileapi_ui/src/app/DesktopTitlebar.tsx  titlebar component owning mode controls and account menu trigger (T-123, T-124, T-148)
fileapi_ui/src/app/AppShell.tsx  app shell layout root, relevant to overall scaling/profile consistency
fileapi_ui/src/settings.css  Settings modal styles incl. control scaling (T-127) and Dropdown migration targets
fileapi_ui/src/features/help/HelpModal.tsx  Help modal component controls to scale with shared tokens (T-128)
fileapi_ui/src/help/help.css  Help modal styles, narrow-width rules (T-128)
fileapi_ui/src/log-view.tsx  LogView component; sort/filter/export controls to scale (T-129)
fileapi_ui/src/log-view.css  LogView styles, narrow-width rules (T-129)
fileapi_ui/src/features/queue/QueueModal.tsx  Queue modal component controls to scale (T-130)
fileapi_ui/src/features/viewer/ViewerModal.tsx  Viewer modal component controls to scale (T-131)
fileapi_ui/src/resizable-pane.tsx  draggable pane resize-handle logic (T-081)
fileapi_ui/src/starship-bridge.css  global body font-family token application (T-016), reduced-motion rule
fileapi_ui/src/styles/vnc-interactions.css  VNC collapse/toolbar interaction styles (T-119-122)
fileapi_ui/src/styles/index.css  ordered global style entry point (T-018)
fileapi_ui/src/webui-shell.css  global shell styles, not directly touched but part of import graph
.opencode/skills/ui-ux-pro-max/SKILL.md  design-system research skill invoked by T-001/T-147 checklist audits
.opencode/skills/ui-ux-pro-max/scripts/search.py  script used to run the design-system search referenced by T-001

APIS
Now I have enough to produce the APIS list.

isMobileViewport  (viewport: ViewportSize) => boolean — shared mobile-breakpoint resolver, true if width ≤1024 or height ≤768
MOBILE_MAX_WIDTH  const 1024, breakpoints.ts px width threshold for mobile layout
MOBILE_MAX_HEIGHT  const 768, breakpoints.ts px height threshold for mobile layout
ViewportSize  type `{ width: number; height: number }` param for isMobileViewport
uiProfile  DesktopApp state `DesktopSettings["uiProfile"]`, values `"auto" | "mobile"`
setUiProfile  React state setter for uiProfile in main.tsx
changeUiProfile  `(profile: DesktopSettings["uiProfile"]) => void` — updates uiProfile state and persists to localStorage under desktopSettingsKey
DesktopSettings  type in main.tsx incl. `uiProfile`, `theme`, `accentColor`, `collapseMainPaneEnabled`, `proxmoxVncModeEnabled`, `shareLinkMode`, `operationLogEnabled`
loginMobileLayout  computed boolean `uiProfile === "mobile" || (uiProfile === "auto" && isMobileViewport(loginViewport))`
mobileLayout  computed boolean (explorer) `uiProfile === "mobile" || (desktopSettings.uiProfile === "auto" && isMobileViewport(viewport))`
desktopSettingsKey  localStorage key string used to persist DesktopSettings (uiProfile etc.)
ui-profile-${uiProfile}  CSS class applied to root element (`.login`, `.explorer`) reflecting current profile, targeted by login.css/tls.css/mobile-ui.css selectors
ui-layout-mobile / ui-layout-desktop  CSS class applied alongside ui-profile-* reflecting resolved mobile/desktop layout
Dropdown  React component in ui/Dropdown.tsx, props `{ label, value, options: DropdownOption[], onChange: (value:string)=>void, disabled?, placeholder?, className? }`
DropdownOption  type `{ value: string; label: string; disabled?: boolean }`
MobileChoiceMenu  React component in ui/MobileChoiceMenu.tsx, props `{ label, currentId, options: MobileChoice[], onSelect: (id:string)=>void, className? }`
ContextPicker  React component in context-picker.tsx, props `{ label, value, groups: ContextPickerGroup[], onSelect: (id:string)=>void, disabled? }`
--font-size-base / --font-size-small / --font-size-heading  CSS vars in tokens.css using `clamp()` for fluid Auto-profile typography
--font-size-mobile-base / --font-size-mobile-small  CSS vars, fixed 16px/14px floors for mobile/Large profile text
--control-height-base  CSS var `clamp(30px, calc(24px + 0.7vmin), 40px)` fluid Auto-profile control height
--control-height-mobile  CSS var fixed 40px control height floor for mobile/Large profile
--control-gap-base / --panel-padding-base  CSS vars, fluid clamp()-based spacing tokens for Auto profile
--icon-size  CSS var, 18px default icon sizing token
--control-focus-ring / --dropdown-focus-ring  CSS vars defining shared focus-ring style
--z-dropdown / --z-context / --z-modal / --z-toast  CSS vars, overlay-registry.ts z-index layering contract (400/450/500/600)
--dropdown-trigger-radius / --dropdown-menu-radius / --dropdown-border / --dropdown-surface / --dropdown-option-radius / --dropdown-option-padding  CSS vars, shared dropdown-like popover visual contract token set

CONTEXT
I have enough. Compiling final CONTEXT section.

- `.planning/ui-baseline/CSS-IMPORTS.md` (T-004 audit) is the authoritative map of which CSS file each task range owns: `styles/tokens.css`→T-008, `desktop-ui.css`→T-028–T-034/T-152, `styles/mobile-ui.css`→T-020–T-023/T-033–T-034, `location-control.css`→T-081–T-098, `styles/commandbar.css`→T-092–T-098/T-125/T-153–T-159, `settings.css`→T-044/T-045/T-061–T-063/T-127, `account-menu.css`→T-124, `styles/mode-switcher.css`→T-123, `styles/vnc-interactions.css`→T-119–T-122, plus five lazy-loaded chunk CSS files (`rest-api.css`, `proxmox-vnc.css`, `log-view.css`, `help/help.css`, `ui/mobile-choice-menu.css`, `ui/dropdown.css`) each tied to their own React.lazy component and specific task ranges — confirms the CSV covers 23 CSS files total with no orphans.
- `.planning/ui-baseline/IN-PROGRESS-T040-T049.md` documents an uncommitted-at-time-of-writing batch (T-040/041/043–049) that replaced hardcoded icon pixel sizes with `var(--icon-size)` in `fileapi_ui/src/ui/icons.tsx`, and lists exactly which glyphs in `main.tsx`, `context-picker.tsx`, `rest-api.tsx`, `proxmox-vnc.tsx`, `log-view.tsx` were touched — any CSV rewrite must reconcile against whichever of these tasks actually landed (git log/commit `56ff8dd` is the last confirmed commit referenced).
- `fileapi_ui/src/desktop-ui.css` is the actual owner of Auto-profile fluid scaling (`.explorer` block: `--ui-font-size`, `--ui-button-height` etc. all `clamp()`/`vmin`-based) and Mobile-profile fixed sizing (`.explorer.ui-layout-mobile` block overrides the same variables with literal px values from `styles/tokens.css`'s `--font-size-mobile-base: 16px` / `--control-height-mobile: 40px`, plus inline literals `12px`, `8px`, `180px`) — this is concrete evidence that "Large profile scales everything uniformly" is NOT yet true in code, contradicting nothing in SUPERSEDED-DENSITY-TASKS.md (which only concerns the removed manual density tiers, not the Mobile/Large profile itself).
- A deliberate, documented removal of a `@media (max-width: 720px)` block from `.explorer` (desktop-ui.css) exists specifically because `isMobileViewport()` in `fileapi_ui/src/styles/breakpoints.ts` (`width <= 1024 || height <= 768`) makes any viewport ≤720px already classified as mobile — so any width/height breakpoint in a rewritten task must stay consistent with the single 1024×768 boundary, not introduce a second one.
- `main.tsx` currently types `uiProfile` as `"auto" | "mobile"` (lines 210, 769, 867–881, 1007–1009) and derives CSS classes `ui-profile-${uiProfile}` and `ui-layout-${mobileLayout ? "mobile" : "desktop"}` (lines 797, 5651) — renaming "Mobile" to "Large" per the task's constraint would require touching this literal string type, the `localStorage`-persisted value under `desktopSettingsKey`, and the label text `"Mobile (Touch Friendly)"` at `main.tsx:820`, plus every CSS selector keyed on `.ui-profile-mobile` / `.ui-layout-mobile` across `desktop-ui.css`, `styles/mobile-ui.css`, `location-control.css`, `proxmox-vnc.css`, `rest-api.css`, `explorer-parity.css`, `login.css`, `styles/mode-switcher.css`, `ui/mobile-choice-menu.css` (dozens of call sites found via grep) — a rename is a wide, multi-file mechanical change, not a single-file edit.
- `fileapi_ui/src/styles/tokens.css` already defines the Auto-profile fluid base tokens referenced by T-028/T-029 (`--font-size-base`, `--control-height-base`, etc., all `clamp()`), separate from the fixed `--font-size-mobile-base`/`--control-height-mobile` tokens used by the Mobile/Large profile — a "Large profile fluid scaling" implementation would need new clamp-based tokens here (or reuse of the Auto ones with a larger ceiling), which don't currently exist.
- `.gitignore` line 44 (`issue*`) means `issue-194-mobile-ui-task-list.csv` is untracked by git; prior edits to it were done in-place with manual `/tmp` backups (per `AUDIT-T042-T074-catchup.md`), not via git history — so there is no git diff/log to inspect for the CSV's edit history, only the file's current on-disk content (which this research pass could not read/enumerate).
- `.planning/ui-baseline/SUPERSEDED-DENSITY-TASKS.md` explicitly warns that T-030/T-031/T-032/T-141/T-152 are `WONT_DO` because they describe the removed manual `uiDensity` feature (compact/standard/comfortable, commit `a82e1b3`) — any rewritten CSV must not resurrect these as if reopening them addresses the new "Large profile scales everything" requirement, since that requirement is about the separate `uiProfile: "mobile"` value, not `uiDensity`.

VERIFIED-TOOLING
  cd fileapi_ui && tsc --noEmit
  cd fileapi_ui && tsc --noEmit
  cd fileapi_ui && tsc --noEmit && vite build
  cd fileapi_ui && vite
  node --test --test-concurrency=1

## grill Q&A

Q1: Should "Mobile" be renamed to "Large" across all identifiers (uiProfile value, localStorage-persisted value, ui-profile-mobile/ui-layout-mobile CSS classes/selectors in all listed files, and the type literal), or should only the user-facing label text change while uiProfile, data-ui-profile, and CSS class names keep using "mobile" internally?
A1: 不用，為了代碼不要修改的太大，只有登入首頁的顯示要改名而已
Q2: Should the rewrite overwrite issue-194-mobile-ui-task-list.csv in place with the same task IDs/row count preserved, or may the rewrite add/remove/renumber rows (and/or produce a separate output file) to fully capture the new unified-style requirements?
A2: overwrite issue-194-mobile-ui-task-list.csv in place, preserving all existing DONE/WONT_DO task_id/test_case_id values verbatim, but allow adding new rows (with new unique T-###/TC-### IDs continuing the existing numbering) or splitting/renumbering only PENDING rows as needed to fully capture the unified-style requirements — do not renumber or remove already-DONE/WONT_DO rows. (auto)
Q3: Should the already-DONE T-028/T-029 (Auto-profile fluid clamp scaling) and T-040/T-041 (icon var(--icon-size) migration) rows be treated as fully satisfying the "everything scales continuously" requirement, or does research showing desktop-ui.css's .explorer.ui-layout-mobile block still uses fixed px overrides (not clamp-based) for the Mobile/Large profile mean new PENDING rows must be added specifically to convert that Mobile/Large profile block to token/clamp-driven fluid scaling?
A3: 能用中文問問題嗎？

## spec

GOAL
Rewrite `issue-194-mobile-ui-task-list.csv` in place so that its rows (preserving the existing CSV schema `task_id,test_case_id,status,priority,mode,implementation_task,existing_surface,expected_behavior,files,verification,blocked_by`) converge the desktop app (`fileapi_ui/`) on one unified visual/interaction style: all sizing driven by `fileapi_ui/src/styles/tokens.css` and `fileapi_ui/src/styles/breakpoints.ts` (no hardcoded absolute pixel positions/sizes), the "Auto" profile using continuous clamp-based fluid scaling with no discrete breakpoint jumps, and the "Mobile" profile (internal identifiers unchanged; only the login-screen display label becomes "Large") scaling every text, control, icon, and spacing value uniformly — while remaining fully usable at a minimum 800×600 viewport for both profiles. Every already-DONE/WONT_DO row must be preserved verbatim; PENDING rows may be added (with new sequential T-###/TC-### IDs) or reworded/split to capture these requirements, keeping task_id/test_case_id uniqueness, valid `blocked_by` chains, and coverage of every file this work touches.

Whether `desktop-ui.css`'s `.explorer.ui-layout-mobile` fixed-px overrides must be converted to token/clamp-driven values is an open question (the user deflected Q3 rather than answering it) — do not assume this conversion is required; if included, flag it explicitly as a judgment call rather than treating it as settled scope. Similarly, do not assert as fact that T-030/T-031/T-032/T-141/T-152 belong to a separate "removed `uiDensity` feature" distinct from the `uiProfile: 'mobile'` (Large) requirement — this distinction was never confirmed by the user; treat it as an open question and do not silently decide it.

CONSTRAINTS
- Preserve the CSV column schema exactly: `task_id,test_case_id,status,priority,mode,implementation_task,existing_surface,expected_behavior,files,verification,blocked_by`.
- Do not alter or renumber any row currently marked `DONE` or `WONT_DO`; carry those rows forward verbatim (task_id, test_case_id, status, and content unchanged) unless code inspection proves the work regressed.
- New or reworded rows must use new, unique, sequentially-continuing `T-###`/`TC-###` IDs; never reuse or collide with existing IDs.
- Per user decision (Q1): do NOT rename `uiProfile` values, `localStorage`-persisted values, `ui-profile-mobile`/`ui-layout-mobile` CSS classes/selectors, or the TypeScript literal type (`"auto" | "mobile"`) anywhere in code. Only the user-facing label text shown on the login screen changes from "Mobile (Touch Friendly)" to reference "Large" — scope any rename task strictly to that login-screen display string.
- Per user decision (Q2): overwrite `issue-194-mobile-ui-task-list.csv` in place; do not produce a separate output file; do not remove or renumber DONE/WONT_DO rows; PENDING rows may be added/split/reworded.
- Every sizing-related expected_behavior in new/reworded rows must specify token-driven (`tokens.css` var) or `clamp()`/fluid-scale behavior — never introduce new fixed-pixel or absolute-position language.
- Preserve all existing concrete identifiers verbatim where still applicable: file paths, `T-###`/`TC-###` IDs, and threshold values (1024×768 mobile boundary from `isMobileViewport` in `breakpoints.ts`, 800×600 minimum viewport, 40px minimum control height, 16px/14px minimum text sizes).
- Q3 is unresolved: whether the `.explorer.ui-layout-mobile` fixed-px block in `fileapi_ui/src/desktop-ui.css` needs a new conversion task, and whether T-030/T-031/T-032/T-141/T-152 pertain to a distinct, already-removed `uiDensity` feature versus the current `uiProfile: 'mobile'` requirement, are both open questions the user did not answer. Do not silently decide either; if the rewrite takes a position on them, mark it explicitly as an assumption pending confirmation rather than as settled fact, and do not reopen or alter the status of T-030/T-031/T-032/T-141/T-152.
- Do not modify `package.json`, `fileapi_ui/package.json`, `fileapi_ui/src-tauri/Cargo.toml`, `fileapi_ui/tsconfig.json`, or `fileapi_ui/vite.config.ts` as part of this work.
- `issue-194-mobile-ui-task-list.csv` is untracked (`.gitignore` line 44: `issue*`); before editing, copy it to `issue-194-mobile-ui-task-list.csv.bak` so status-preservation can be verified.
- Do not fabricate design rules absent from `.planning/` or the codebase; where `.planning/` content is unavailable, do not invent design-system specifics.

ACCEPTANCE
- `issue-194-mobile-ui-task-list.csv` still has the exact original header/column order.
- Every row that was `DONE` or `WONT_DO` in the pre-edit backup has an identical `task_id`, `test_case_id`, and `status` in the rewritten file, including T-030/T-031/T-032/T-141/T-152 unchanged.
- All `task_id` and `test_case_id` values in the rewritten file are unique.
- Every `blocked_by` reference resolves to a `task_id` that exists in the file.
- The CSV contains a row scoping the "Mobile (Touch Friendly)" → "Large" rename strictly to the login-screen display label (not internal `uiProfile`/class identifiers), consistent with the constraint above.
- No row's `expected_behavior` text describes a new hardcoded pixel position/size; sizing language references tokens or `clamp()`.
- Every new or reworded row's `files` field lists concrete, existing file paths under `fileapi_ui/` that the row's `expected_behavior` actually describes changing; no row references a file it does not touch.

VERIFY:
```sh
cd /Users/ascopia/Project/filetransfer_ui
test -f issue-194-mobile-ui-task-list.csv
head -1 issue-194-mobile-ui-task-list.csv | grep -qx 'task_id,test_case_id,status,priority,mode,implementation_task,existing_surface,expected_behavior,files,verification,blocked_by' && echo "header OK"
python3 - <<'PY'
import csv, sys, os
with open("issue-194-mobile-ui-task-list.csv", newline="") as f:
    rows = list(csv.DictReader(f))
ids = [r["task_id"] for r in rows]
tcids = [r["test_case_id"] for r in rows]
assert len(ids) == len(set(ids)), "duplicate task_id found"
assert len(tcids) == len(set(tcids)), "duplicate test_case_id found"
idset = set(ids)
for r in rows:
    dep_field = (r.get("blocked_by") or "").strip()
    if not dep_field:
        continue
    for dep in dep_field.replace(";", ",").split(","):
        dep = dep.strip()
        if dep:
            assert dep in idset, f"{r['task_id']} has unresolved blocked_by dep '{dep}'"
# verify files listed in each row actually exist on disk
for r in rows:
    files_field = (r.get("files") or "").strip()
    if not files_field:
        continue
    for fp in files_field.replace(";", ",").split(","):
        fp = fp.strip()
        if fp and not os.path.exists(fp):
            print(f"WARNING: {r['task_id']} references missing file '{fp}'", file=sys.stderr)
print(f"OK: {len(rows)} rows, unique IDs, all blocked_by resolved")
PY
if [ -f issue-194-mobile-ui-task-list.csv.bak ]; then
python3 - <<'PY'
import csv
def load(path):
    with open(path, newline="") as f:
        return {r["task_id"]: r["status"] for r in csv.DictReader(f)}
old = load("issue-194-mobile-ui-task-list.csv.bak")
new = load("issue-194-mobile-ui-task-list.csv")
changed = [tid for tid, st in old.items() if st in ("DONE", "WONT_DO") and new.get(tid) != st]
assert not changed, f"DONE/WONT_DO status changed for: {changed}"
for tid in ("T-030", "T-031", "T-032", "T-141", "T-152"):
    if tid in old:
        assert new.get(tid) == old.get(tid), f"{tid} status must remain unchanged pending Q3 resolution"
print("OK: all prior DONE/WONT_DO rows preserved, including T-030/T-031/T-032/T-141/T-152")
PY
else
  echo "WARNING: no backup found at issue-194-mobile-ui-task-list.csv.bak; cannot verify DONE/WONT_DO preservation" >&2
fi
grep -qi "Large" issue-194-mobile-ui-task-list.csv && echo "OK: Large label rename referenced in CSV"
echo "Manual check: confirm no new row introduces fixed-px sizing language without token/clamp reference"
```

## phase timings

refine               54.6s
research            240.9s
  enrichment             656ms
  worker:files wait      473ms
  worker:files work      91.6s
  worker:apis wait       617ms
  worker:apis work       54.6s
  worker:context wait    481ms
  worker:context work    73.4s
  worker:tooling wait    541ms
  worker:tooling work     9.2s
  workers               231.7s
  verify-tooling          9.2s
grill               172.9s
  gen                     4.6s
  auto-answer             7.9s
  gen                     9.3s
  auto-answer             5.2s
  gen                     5.5s
  auto-answer             6.1s
  gen                     6.3s
  gen                     5.3s
compose              48.1s
critique             61.8s
  triage                 33.5s
  rewrite                28.4s
total               578.3s

## handoff

handoff_at: 2026-08-16T11:18:41.041Z

## gates

- 2026-08-16T11:41:46.727Z verify: PASS
